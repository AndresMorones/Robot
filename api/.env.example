# API authentication.
# Generate a strong token via: openssl rand -hex 32
API_BEARER_TOKEN=replace-with-strong-token

# Load data file (relative to api/ working directory).
LOADS_JSON_PATH=../data/loads.json

# FMCSA QCMobile web key (used in WS2b — placeholder for now).
FMCSA_WEB_KEY=

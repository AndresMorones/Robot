# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
*.egg-info/
dist/
build/

# Environment files (uv.lock IS committed; see no entry below)
.env
.env.local
.env.*.local

# Local secrets (NEVER commit)
secrets.local.md
*.secrets.local
.secrets/

# Runtime data (local-only; production lives on Fly volume)
data/calls.json
data/calls.json.lock
data/calls.json.tmp

# Twin REST mirror cache (generated by scripts/sync_twin_loads.py)
data/twin_mirror/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code internal config + memory (per-developer, never committed)
.claude/
.mcp.json
sess_messages.json

# Local scratch + audit outputs (per-developer, never committed)
tmp/
tmp_audit/

# HappyRobot raw vendor docs (belt-and-suspenders; KB lives outside repo at C:\Users\Andre\happyrobot-kb\)
docs/references/happyrobot/Happyrobot docs.*
.happyrobot.env
